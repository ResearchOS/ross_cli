[build-system]
requires = [
    "hatchling",
]
build-backend = "hatchling.build"

[tool.ross]
language = "python"
dependencies = []

[project]
name = "ross_cli"
version = "0.2.1"
authors = []
readme = "README.md"
dependencies = [
    "tomli",
    "tomli_w",
    "typer"
]
